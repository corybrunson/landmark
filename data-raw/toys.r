# interval with a double-point farther from a center than two distinct points
peg <- matrix(c(
  0, 1, 1, 3, 4, 5, 6
))
usethis::use_data(peg)

# interval with different densities close to endpoints
bar <- matrix(c(
  -1, -.5, 0,
  .75, .875, 1
))
usethis::use_data(bar)
